import { usersCollectionRef } from "./firebase";

/*
db = firebase.firestore();
usersRef = db.collection("users");

user = usersRef.doc(uid);

user field => userInformation

itemCollections = usersRef.doc(uid).collection("itemCollections").doc(collectionUID)

itemCollections field => color, collaborators...

item = itemCollections.collection("item").doc(itemID)

item field => text, isComplete...
*/
